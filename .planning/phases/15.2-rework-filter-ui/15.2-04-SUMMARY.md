# Phase 15.2 Plan 04: UI Accessibility & UX Fixes Summary

**One-liner:** Fixed dropdown accessibility (type="button", role="menuitem") and added auto-close behavior on option selection

## Changes Made

### Task 1: Add type='button' to all buttons (GAP 9)
- Added `type="button"` to direction toggle Button in FilterSortControl.tsx
- Added `type="button"` to filter option Buttons in FilterSortControl.tsx
- Added `type="button"` to sort option Buttons in FilterSortControl.tsx
- Added `type="button"` to Clear Filters Button in PromptListView.tsx

### Task 2: Close dropdown after selection (GAP 10)
- Added `setIsOpen(false)` to filter option onClick handler
- Added `setIsOpen(false)` to sort option onClick handler
- Direction toggle intentionally keeps dropdown open (users often toggle direction then select sort option)

### Task 3: Add ARIA menuitem roles (GAP 11)
- Added `role="menuitem"` to filter option Buttons
- Added `role="menuitem"` to sort option Buttons
- Container already had `role="menu"` - now properly associated with menu items

## Files Modified

| File | Changes |
|------|---------|
| src/components/FilterSortControl.tsx | Added type="button", role="menuitem", setIsOpen(false) |
| src/components/PromptListView.tsx | Added type="button" to Clear Filters button |

## Verification Results

- Build: PASSED (no TypeScript errors)
- Lint: PASSED (only pre-existing Fast Refresh warnings)
- All Button components have explicit type="button"
- 2 occurrences of role="menuitem" (filter and sort options)
- 1 occurrence of role="menu" (container)
- setIsOpen(false) in filter and sort onClick handlers

## Gaps Closed

- **GAP 9:** Buttons should have explicit type="button" - RESOLVED
- **GAP 10:** Dropdown stays open after selection - RESOLVED
- **GAP 11:** Menu items lack role="menuitem" - RESOLVED

## Commits

| Hash | Message |
|------|---------|
| 8e985bd | fix(15.2-04): improve dropdown accessibility and UX (Gaps 9, 10, 11) |

## Duration

~4 minutes

## Deviations from Plan

None - plan executed exactly as written.
